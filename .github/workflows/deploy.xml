name: Deploy React Portfolio

on:
  push:
    branches: ["main"]

permissions:
  contents: write

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Baixar código
        uses: actions/checkout@v4

      - name: Configurar Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20' # Use a versão do node que você usa

      - name: Instalar dependências
        run: npm ci # Ou npm install

      - name: Buildar o projeto
        run: npm run build # O comando que gera a pasta dist ou build

      - name: Fazer Deploy no GitHub Pages
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./dist # Se for React padrão, mude para ./build